export const environment = {
  production: true,
  apiBaseUrl: 'https://todo-group-app.herokuapp.com/user-tasks',
  apiEndpoints: {
    user: '/users',
    auth: {
      local: '/auth/local'
    }
  }
};
